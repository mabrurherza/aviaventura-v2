---
const { title, excerpt, airline, thumbnail, slug } = Astro.props;
---

<div class="flex flex-col gap-12 lg:flex-row">
  <div class="bg-slate-50 relative">
    <img
      src={thumbnail}
      alt={`Office preview of ${title}`}
      class="rounded-lg aspect-video lg:aspect-square object-cover lg:w-96"
    />
  </div>
  <div class="border-b w-full flex flex-col justify-between">
    <div class="flex flex-col gap-2">
      <p class="uppercase text-slate-400 font-semibold tracking-wider">
        Partnership with {airline}
      </p>
      <h3 class="text-3xl text-brand hover:text-sky-600">
        <a href={`/casestudies/${slug}`}>{title}</a>
      </h3>
      <p>{excerpt}</p>
    </div>
    <a
      href={`/casestudies/${slug}`}
      class="text-lg text-sky-600 hover:text-sky-700 underline py-5"
      >Read more</a
    >
  </div>
</div>
