---
const { eyebrow, heading, subparagraph, advantages } = Astro.props;
import Eyebrow from "../Eyebrow.astro";

interface Props {
  eyebrow: string;
  heading: string;
  subparagraph: string;
  advantages: string[];
}
---

<section class="bg-brand text-white pb-32">
  <div class="max-w-360 px-5 mx-auto flex flex-col gap-6 lg:gap-20 lg:flex-row">
    <div class="flex-1 flex flex-col max-w-150">
      <Eyebrow text="DEDICATED TEAM ADVANTAGES" />

      <p class="text-base sm:text-xl">
        {subparagraph}
      </p>
    </div>
    <div class="flex flex-1 flex-col gap-10">
      <ul class="advantages flex flex-col text-xl sm:text-2xl">
        {
          advantages.map((advantage) => (
            <li class="flex gap-2 py-6 md:py-10 border-b border-slate-200/30">
              <img
                src="/img/checklist.svg"
                alt="Checklist"
                class="h-5 sm:h-8"
              />
              {advantage}
            </li>
          ))
        }
      </ul>
    </div>
  </div>
</section>
